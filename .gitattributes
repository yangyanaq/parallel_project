# Todo texto con LF: los .sh/.py/fuentes se ejecutan y compilan en Linux (RPi/Jetson).
# CRLF rompería run_benchmarks.sh en los clústeres.
* text=auto eol=lf

# Excepciones: scripts nativos de Windows
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binarios
*.bin binary
*.png binary
*.pdf binary
